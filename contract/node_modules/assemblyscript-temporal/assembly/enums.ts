export const enum Overflow {
  Reject,
  Constrain,
}

export const enum Disambiguation {
  Compatible,
  Earlier,
  Later,
  Reject,
}

export const enum TimeComponent {
  Years,
  Months,
  Weeks,
  Days,
  Hours,
  Minutes,
  Seconds,
  Milliseconds,
  Microseconds,
  Nanoseconds,
}
